spring.application.name=match-predictor

# Server Configuration
server.port=8080

# Ollama AI Configuration
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3.2
spring.ai.ollama.chat.temperature=0.7

# PostgreSQL Database Configuration (CHANGED FROM H2)
spring.datasource.url=jdbc:postgresql://localhost:5432/match_prediction_db
spring.datasource.username=postgres
spring.datasource.password=1q2w3e
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Security Configuration (temporary - disable for now)
spring.security.user.name=admin
spring.security.user.password=password
spring.security.user.roles=USER

# Logging
logging.level.com.example.matchpredictor=DEBUG
logging.level.org.springframework.ai=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE